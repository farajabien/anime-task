import React, { FC } from 'react'
import './App.css'
import { AnimeList } from './components/AnimeList'

const App: FC = () => {
	return (
		<div className='App'>
			<AnimeList />
		</div>
	)
}

export default App
